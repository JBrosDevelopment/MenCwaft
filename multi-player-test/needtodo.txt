okay so, i need this to use firebase as the signaller and webrtc as the connection between the server and clients. It needs to support multiple clients on one server. the server id needs to be customizeable, and the clients need to be able to choose which server they want to connect to by typing in the server id. there needs to be able to have multiple servers running so that multiple clients can choose which server. t needs to be able to support the different clients being different tabs on the same computer, and different clients on different computers, all connected to the same host. the test idea project i have set up is for a chat room for each of the clients to communicate to the server, and the server will echo the message to all the different clients.